// Contains the names for the ships based on size
const shipNames = {
  2: "Sentinel Probe",
  a3: "Assault Titan",
  b3: "Viper Mech",
  4: "Iron Goliath",
  5: "Leviathan",
};

// Factory that can create and return one of a variety of pre-determined ships.
const Ship = () => {
  // Create the ship object that will be returned
  const newShip = {};

  // Set up ship based on params

  return newShip;
};
export default Ship;
